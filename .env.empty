# X-get2put（空 .env 模板）
# 说明：推荐“小白”直接在网页面板里填写并保存（不需要改 .env）。
# 只有当你没有在网页里保存配置（或对应字段为空）时，程序才会尝试读取这里的环境变量作为兜底。

# =========================
# 监控（读数据）：twitterapi.io
# =========================
# 请求头：X-API-Key
TWITTERAPI_IO_KEY=

# 兼容旧变量名（如你之前已经在用，可以不改）
TWITTERAPI_IO_API_KEY=

# 可选：默认就是 https://api.twitterapi.io
TWITTERAPI_IO_BASE_URL=https://api.twitterapi.io

# 监控配置
# 多个账号用逗号或换行分隔：elonmusk,naval
MONITOR_TARGETS=

# 轮询间隔（秒），推荐 1800（30 分钟轮询一次，省额度更稳定）
MONITOR_POLL_INTERVAL_SEC=1800

# 每次抓取条数（上限）；接口单次默认返回 20 条，>20 会自动翻页（调用次数也会增加）
MONITOR_FETCH_LIMIT=20

MONITOR_INCLUDE_REPLIES=false

# 引用推文（quote，带评论的转发）是否参与搬运；false 表示跳过 quote
MONITOR_INCLUDE_QUOTE_TWEETS=true

# 文本中包含 “@” 则跳过（不入队/队列中也会丢弃）
MONITOR_SKIP_MENTIONS=false

# =========================
# 转发（写数据）：官方 X API
# =========================
# twitter-api-v2（OAuth 1.0a 四件套）
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# 你的本地梯子（可选）
HTTPS_PROXY=
HTTP_PROXY=
ALL_PROXY=

# 转发控制
# 只在 FORWARD_ENABLED=true 且 FORWARD_DRY_RUN=false 时会真正发推
FORWARD_ENABLED=false
FORWARD_DRY_RUN=true

# retweet / create_tweet
FORWARD_MODE=create_tweet

# 一次发现多条新推时，每条间隔 N 秒发一条（推荐 5）
FORWARD_SEND_INTERVAL_SEC=5

# 开启后：搬运发新推（create_tweet）时，先翻译为中文再发布
FORWARD_TRANSLATE_TO_ZH=false

# 可选：服务端口（如果被占用，服务会自动尝试 3001/3002...）
PORT=3000

# 仅 exe 免安装版有效：启动后自动打开浏览器（默认 true）
AUTO_OPEN_BROWSER=true

# 自动化 smoke test（可选）
SMOKE_USERNAME=
SMOKE_PORT=0
SMOKE_TIMEOUT_MS=45000

# =========================
# 保留字段（当前版本不使用）
# =========================
# 旧版 twitterapi.io 登录/发推相关（已弃用）
TWITTER_AUTH_SESSION=
TWITTER_PROXY=

# 旧版“登录换 cookie”相关（已弃用）
TWITTER_LOGIN_PROXY=
TWITTER_LOGIN_USERNAME_OR_EMAIL=
TWITTER_LOGIN_PASSWORD=
TWITTER_LOGIN_2FA_CODE=
